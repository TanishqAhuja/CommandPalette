/**
 * Adapters barrel export.
 * Each Adapter is independent and follows single responsibility principle.
 */

export { createToastAdapter } from "./toastAdapter";
export type { ToastAdapter } from "./toastAdapter";
